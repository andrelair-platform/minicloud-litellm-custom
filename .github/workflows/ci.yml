name: Build and Push

on:
  push:
    branches: [main, staging, dev]
    paths:
      - 'Dockerfile'
      - '**.py'
  pull_request:
    branches: [main, staging]
    paths:
      - 'Dockerfile'
      - '**.py'
  workflow_dispatch:

env:
  HARBOR_REGISTRY: harbor.10.0.0.200.nip.io
  IMAGE_NAME: library/litellm-custom
  GITOPS_IMAGE: harbor.10.0.0.200.nip.io/library/litellm-custom

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Compute image tag
        id: meta
        run: |
          VERSION=$(grep 'LITELLM_VERSION' Dockerfile | sed 's/.*=//')
          BASE="${VERSION:-$(git rev-parse --short HEAD)}"
          BRANCH=${GITHUB_REF_NAME}
          if [ "$BRANCH" = "main" ]; then
            TAG="${BASE}"
          else
            TAG="${BRANCH}-${BASE}"
          fi
          echo "tag=${TAG}"       >> $GITHUB_OUTPUT
          echo "branch=${BRANCH}" >> $GITHUB_OUTPUT

      # ── Tailscale: join the minicloud tailnet so we can reach Harbor ────────
      - name: Connect to Tailscale
        if: github.event_name != 'pull_request'
        uses: tailscale/github-action@v3
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}
          version: latest

      # ── Trust the minicloud self-signed CA so Docker accepts Harbor TLS ────
      - name: Trust minicloud CA
        if: github.event_name != 'pull_request'
        run: |
          sudo mkdir -p /etc/docker/certs.d/${{ env.HARBOR_REGISTRY }}
          echo "${{ secrets.MINICLOUD_CA_CERT }}" | sudo tee /etc/docker/certs.d/${{ env.HARBOR_REGISTRY }}/ca.crt
          echo "${{ secrets.MINICLOUD_CA_CERT }}" | sudo tee /usr/local/share/ca-certificates/minicloud-ca.crt
          sudo update-ca-certificates

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."harbor.10.0.0.200.nip.io"]
              insecure = true

      - name: Login to Harbor
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_USER }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ env.HARBOR_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install Cosign
        if: github.event_name != 'pull_request' && github.ref_name != 'dev'
        uses: sigstore/cosign-installer@v3

      - name: Sign image
        if: github.event_name != 'pull_request' && github.ref_name != 'dev'
        run: |
          cosign sign --yes \
            ${{ env.HARBOR_REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}

      - name: Update image tag in minicloud-gitops (main only)
        if: github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITOPS_TOKEN }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          NEW_TAG: ${{ steps.meta.outputs.tag }}
        run: |
          git clone https://x-access-token:${GH_TOKEN}@github.com/andrelair-platform/minicloud-gitops.git
          cd minicloud-gitops
          git config user.name 'AndreLiar'
          git config user.email 'andrelaurelyvan.kanmegnetabouguie@ynov.com'
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import 2>/dev/null || true
          git config commit.gpgsign true
          git config user.signingkey FD6D39D681DEFA34
          sed -i "s|harbor\.[^/]*/library/litellm-custom:.*|${GITOPS_IMAGE}:${NEW_TAG}|g" \
            manifests/ai/01-litellm-deployment.yaml
          git add manifests/ai/01-litellm-deployment.yaml
          git diff --cached --quiet || \
            git commit -S -m "ci(litellm): bump image to ${NEW_TAG}"
          git push
